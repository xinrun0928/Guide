---
description: VitePress 知识库文档编写规范 — 强调趣味性、渐进性、读者兴趣激发，以及面试专题写法
globs: docs/**/*.md
alwaysApply: true
---

# VitePress 知识库文档编写规范

> 核心目标：让每一篇文档都有「钩子」，让读者忍不住一口气读完

---

## 核心理念：从「灌知识」到「点燃好奇心」

好的技术文档不是教科书，不是 API 手册，更不是面试题库的堆砌。

它是**一场对话**——你在引导读者一步步揭开技术的面纱，在关键处停顿、抛出问题、给出悬念，直到最后让读者发出「原来如此」的感叹。

读者为什么会点开这篇文章？他可能是在准备面试，可能是在排查 bug，也可能只是随便逛逛。**你的任务是让他留下来，然后一直读下去。**

---

## 一、开场：用「前 3 秒」抓住读者

读者的时间有限，耐心更有限。如果开场无聊，他 3 秒就关掉了。

### 八种开场方式

#### 1. 场景切入——让读者「身临其境」

```markdown
凌晨 2 点，你正准备上线新功能，数据库连接池突然耗尽。
日志里全是 `Connection timeout`，但你没改任何配置。
这是怎么回事？
```

> **为什么有效**：把读者拉进具体场景，他会产生「这就是我」的代入感。

#### 2. 痛点共鸣——说出读者想说但没说出口的话

```markdown
你一定遇到过这种情况：synchronized 用了，代码还是有问题。
加了 volatile，线程 A 修改的值，线程 B 还是读不到。
——问题到底在哪？
```

> **为什么有效**：读者会觉得「你懂我」，从而产生信任感。

#### 3. 核心追问——用问题撕开知识的面纱

```markdown
# HashMap.get() 的时候，JVM 到底做了什么？

大多数人只会说「调用 hashCode()，然后找到对应位置」。
但追问下去，十个人里有九个答不上来。
```

> **为什么有效**：人天生讨厌「不知道」的感觉，求知欲会驱使他读下去。

#### 4. 反差对比——打破固有认知

```markdown
很多人说：synchronized 性能很差，能不用就不用。

但 JDK 6 之后，JVM 团队花了整整三年优化它。
现在的 synchronized，性能已经可以和 Lock 持平。
```

> **为什么有效**：反差制造认知冲突，而冲突是注意力最强的时刻。

#### 5. 数字锚定——用数据说话

```markdown
90% 的 MySQL 慢查询，归结于这三个原因。

掌握它们，能让你的查询从 10 秒降到 50 毫秒。
```

> **为什么有效**：具体的数字比模糊的描述更有说服力。

#### 6. 故事开场——给技术一个「身世」

```markdown
2014 年，James Gosling 在一次访谈中回忆：
Java 最初的名字是「Green」，目标是为消费电子设备编程。
谁能想到，这个为微波炉设计的语言，后来统治了企业级开发？
```

> **为什么有效**：故事是人类理解世界最原始的方式。

#### 7. 悬念制造——先给结论，再讲原因

```markdown
# HashMap 在 JDK 8 之后，永远不会出现死循环了。

这是真的吗？
```

> **为什么有效**：人天生讨厌「悬而未决」，一定要知道答案。

#### 8. 快速上手——先让读者尝到甜头

```markdown
先看效果：

没加索引：查询 100 万条数据，用了 8.2 秒
加了索引：同样查询，只用了 12 毫秒

怎么做到的？往下看。
```

> **为什么有效**：先给甜头，让读者觉得「值」，他才愿意继续投入时间。

### 开场避坑

| 避坑 | 说明 |
|------|------|
| ❌ 大空话 | 「随着互联网的发展...」「在当今社会...」—— 读者直接关掉 |
| ❌ 声明式开头 | 「本文将介绍...」「本节讲解...」—— 毫无信息量 |
| ❌ 长篇铺垫 | 铺垫三段才进正题——消耗耐心 |
| ❌ 元信息标签 | `> JDK 17+ \| 新手友好 \| 边学边练` —— 像免责声明，劝退读者 |

---

## 二、节奏控制：让读者「一口气读完」

读者阅读的过程就像爬山——你需要控制坡度，让他不觉得累，同时又不断有新风景。

### 慢节奏：核心原理与关键概念

适用于：第一次介绍重要概念、需要深度理解的地方。

```markdown
## synchronized 的锁，到底锁的是什么？

不是锁代码，是锁**对象**。

当线程 A 进入 synchronized 代码块时，它需要在对象头里「做标记」。
这个标记，表明：「这个对象正在被我占用，你们等等」。

但这里有个微妙的地方——synchronized 加在**方法**上和加在**代码块**上，
锁的东西是不一样的，你知道区别在哪吗？
```

> **特点**：自问自答、留有悬念、代码 + 解读配合。

### 快节奏：配置步骤与重复性内容

适用于：环境搭建、配置说明、列表类内容。

```markdown
## MySQL 索引创建

```sql
-- 单列索引
ALTER TABLE orders ADD INDEX idx_user_id (user_id);

-- 联合索引（注意字段顺序）
ALTER TABLE orders ADD INDEX idx_user_status (user_id, status);

-- 查看索引
SHOW INDEX FROM orders;
```
```

> **特点**：简洁、直接、给代码就行。

### 起伏设计原则

```
开场（悬念）→ 基础概念（慢）→ 快速上手（快）→ 深入原理（慢）→ 实战应用（快）→ 留白思考（悬念）
```

一篇 3000 字的文档，至少要有 2-3 次节奏切换。**如果从头到尾都是同一个节奏，读者会睡着。**

---

## 三、渐进式讲解：从「不知道」到「恍然大悟」

这是最核心的写作技巧：**永远不要一下子把所有东西扔给读者。**

### 漏斗式：从大到小，逐步聚焦

```
大领域 → 核心问题 → 关键概念 → 细节展开 → 实践应用

例如讲 HashMap：
1. HashMap 是什么？（键值对存储）
2. 为什么需要它？（数组查询快但插入慢，链表插入快但查询慢）
3. HashMap 怎么解决这个矛盾？（哈希 + 数组 + 链表）
4. JDK 8 又做了什么优化？（链表转红黑树）
5. 实际怎么用？（代码示例 + 注意事项）
```

### 阶梯式：每一步都有小成果

```
第一步：让代码跑起来（最简版本）
第二步：加上错误处理（完整版本）
第三步：优化性能（调优版本）
第四步：解释为什么这样设计（原理版本）
```

每上一个台阶，读者都有「我做到了」的成就感。

### 悬念递进：层层剥开

```
提出问题 → 初步理解 → 发现新问题 → 深入理解 → 彻底掌握

例如讲 volatile：
1. 可见性是什么？（线程 A 修改，线程 B 看不到）
2. synchronized 能解决吗？（能，但太重）
3. volatile 呢？（轻量级解决方案）
4. volatile 底层怎么实现的？（内存屏障）
5. volatile 能替代锁吗？（不能，有些场景不适用）
```

---

## 四、表达技巧：让文字「有性格」

### 善用对比——让差异一目了然

```markdown
# synchronized vs Lock

| 特性 | synchronized | Lock |
|------|-------------|------|
| 获得锁失败 | 阻塞等待 | 可选择超时/放弃 |
| 释放锁 | 自动释放 | 必须手动释放 |
| 公平锁 | 否 | 可配置 |
| 注解支持 | 无 | 有 @Locked |
```

```markdown
别小看这个区别——

synchronized 的自动释放，是优点也是陷阱。
你不小心把 return 放在 try 里？锁早就丢了。
```

### 善用类比——让抽象变具体

```markdown
B+ 树的查询过程，就像在字典里找字：

1. 先看拼音（第 1 层索引）
2. 再翻到对应页码范围（第 2 层索引）
3. 最后找到具体的字（叶子节点）

每翻一页，都是一次磁盘 I/O。
所以树越深，查询越慢——这也是为什么 MySQL 要控制 B+ 树的深度。
```

### 善用转折——打破读者预期

```markdown
大多数教程会告诉你：synchronized 可以解决线程安全问题。

但我想告诉你：**用错了 synchronized，问题更大。**

比如在循环里加锁，每次都要竞争锁——
你以为在保护共享资源，其实是在让线程排队等锁。
```

### 善用数字——让描述更精准

```markdown
# 不要
"HashMap 在数据量大时性能会下降"

# 要
"HashMap 在 JDK 7 中，扩容时 rehash 会重新计算每个元素的哈希位置，
在极端情况下，可能导致 2^n 次重新分布"
```

### 善用引语——增加人情味

```markdown
> "我不在乎代码能不能跑，我在乎的是未来的自己能看懂。" —— 某位不愿透露姓名的开发者

好的命名和注释，不是给机器看的，是给未来的自己留的退路。
```

---

## 五、代码呈现：让代码「会说话」

### 渐进式展示：从坏到好

```markdown
## 这样的 HashMap 用法，有问题

```java
Map map = new HashMap();
map.put("key", "value");
String value = (String) map.get("key");
```

**问题在哪？**

类型安全全部丢掉了，运行时才发现类型错误。
JDK 5 之后，我们有了更好的选择。
```

```markdown
## 改进：用泛型约束类型

```java
Map&lt;String, String&gt; map = new HashMap&lt;&gt;();
map.put("key", "value");
String value = map.get("key");  // 不需要强转了
```

**但还有优化的空间。**

你知道 `new HashMap<>()` 默认容量是多少吗？
答案是 16，负载因子是 0.75。
这意味着什么？
```

```markdown
## 最终形态：考虑性能

```java
// 预估容量 = 期望元素数 / 负载因子 + 1
// 减少扩容次数，提升性能
Map&lt;String, String&gt; map = new HashMap&lt;&gt;(32);
```

三个版本的演变，就是 HashMap 使用进化的缩影。
```

### 代码注释：告诉「为什么」而不是「是什么」

```markdown
# 不要：注释在解释「是什么」
```java
map.get(key);  // 根据 key 获取 value
```

# 要：注释在解释「为什么」和「例外情况」
```java
Object value = map.get(key);
// 注意：如果 key 不存在，返回 null
// 如果允许 null 值，用 map.getOrDefault(key, defaultValue)
```
```

---

## 六、结尾设计：让读者「意犹未尽」

结尾不是总结，是**下一次探索的起点**。

### 类型一：抛出新问题

```markdown
## 留给你的问题

synchronized 的锁升级（无锁 → 偏向锁 → 轻量级锁 → 重量级锁），
是一个不可逆的过程。

但如果一个对象从偏向锁一路升级到重量级锁后，
锁释放了，再次加锁时，会从哪个状态开始？

这个问题，值得你去看一下 HotSpot 源码。
```

### 类型二：给行动清单

```markdown
## 下一步，你可以

- [ ] 用 `jstack` 查看自己项目里的线程状态
- [ ] 分析哪些 synchronized 可以换成 ReentrantLock
- [ ] 用 Arthas 监控锁竞争情况
```

### 类型三：对比回顾，引发思考

```markdown
## 最后留个思考题

我们讲了 synchronized 的锁升级，你知道 Lock 有什么 synchronized 没有的能力吗？

公平锁、可中断获取锁、多条件变量……

下一节，我们来聊聊 Lock 的高级特性。
```

### 类型四：金句升华

```markdown
最后，送你一句话：

> "最好的代码注释，是让未来的自己感谢现在的自己。"

共勉。
```

---

## 七、差异化检查清单

写完文档后，对着这份清单检查：

| 检查项 | 问题 |
|--------|------|
| **开场是否有钩子？** | 能否把前两段去掉后直接放到另一篇文章开头？ |
| **节奏是否单调？** | 全文从头到尾都是一个语速吗？ |
| **是否有渐进感？** | 读者是从「不知道」到「恍然大悟」的过程吗？ |
| **是否有个人风格？** | 读十篇文章，能分辨出是同一个人写的吗？ |
| **是否有记忆点？** | 读者离开后，能记住这篇文章讲了什么吗？ |
| **面试内容是否必要？** | 面试分析是否帮助理解原理，还是喧宾夺主？ |
| **技术是否准确？** | 代码能运行、结论有依据、边界条件说明了吗？ |

---

## 八、内容质量底线

无论风格如何变化，这些标准是底线：

| 标准 | 要求 |
|------|------|
| **准确性** | 技术细节必须正确，有歧义的地方必须说明 |
| **渐进性** | 从简单到复杂，给读者爬台阶的时间 |
| **完整性** | 核心知识点必须有「是什么—为什么—怎么用」 |
| **可运行** | 代码示例必须能直接运行 |
| **好奇心** | 每个知识点结尾，留一个问题让读者思考 |

---

## 九、面试专题写法（非必须）

本项目是 Java 面试指南，可以根据需要在文档中增加面试相关内容，增加实战感。

### 面试官心理分析

在讲解技术点时，可以适当分析面试官的出题意图：

```markdown
## 为什么要问 HashMap 的死循环问题？

面试官问这个问题，不是想让你背答案，而是想看：

1. 你对 JDK 7 和 JDK 8 的区别了解多少？
2. 你有没有排查过生产环境的实际问题？
3. 你的思维方式——是只会用，还是会想「为什么这样设计」？

顺着这个思路回答，比单纯背答案高出几个层次。
```

### 面试题剖析

遇到高频面试题，可以这样展开：

```markdown
## 「synchronized 和 Lock 的区别」——这道题怎么答？

这道题看似简单，但面试官想考察的绝不只是 API 层面的区别。

**表面层（大部分人能答上来）：**
- synchronized 是 JVM 关键字，Lock 是接口
- synchronized 自动释放锁，Lock 需要手动释放

**深层（优秀候选人能答上来）：**
- synchronized 可以修饰方法，Lock 只能锁代码块
- Lock 支持公平锁、非公平锁，synchronized 不支持
- Lock 支持tryLock()，synchronized 不支持

**实战层（让面试官眼前一亮的答案）：**
- 在高并发场景下，synchronized 的锁升级机制可能导致线程阻塞
- 但 JDK 6 之后做了大量优化，两者在大多数场景下性能差异不大
- 真正影响性能的，是锁的粒度，而不是锁的实现方式
```

### 面试追问预判

在知识点结尾，可以给出面试官可能的追问方向：

```markdown
## 总结

synchronized 的核心是对象锁，通过 Mark Word 记录锁状态。

**面试追问方向：**
- synchronized 锁升级的过程是什么？为什么不可逆？
- 偏向锁在什么情况下会撤销？
- 重量级锁的底层是什么？为什么 JDK 6 之后性能大幅提升？

这些追问方向，也是你深入学习的方向。
```

### 面试场景模拟

可以设计一个「面试官问，你答」的模拟环节：

```markdown
## 场景模拟

**面试官：** HashMap 是线程安全的吗？

**候选人甲：** 不是。

**面试官：** 那用什么？

**候选人甲：** ConcurrentHashMap。

**面试官：** 好，下一题。

---

**候选人乙：** 不是。线程安全场景下，HashMap 不适合，有三个原因……

面试官眉毛一挑：「说说看。」

（候选人开始展开……）
```

### 使用原则

| 写法 | 使用频率 | 说明 |
|------|----------|------|
| 面试官心理分析 | 可选 | 每篇选 1-2 个重点知识点加分析 |
| 面试题剖析 | 可选 | 高频题才加，不要每题都剖析 |
| 面试追问预判 | 推荐 | 每个知识点结尾留 2-3 个追问方向 |
| 场景模拟 | 可选 | 用得好加分，用不好显得刻意 |

> **核心原则**：面试内容是加分项，不是必选项。技术原理讲清楚是前提，面试内容是锦上添花。如果为了加面试内容而影响原理讲解，删掉面试部分。

---

## 九、排版细节规范

### frontmatter 头信息

由 AI 生成的 md 文档**不需要**包含 frontmatter 头信息（`---`、`title`、`date` 等），保持纯 Markdown 内容即可。

正确：

```markdown
# 标题

正文内容...
```

错误（无需生成）：

```markdown
---
title: 标题
date: 2024-01-01
---

# 标题

正文内容...
```

## 十、代码语言规范

AI 生成的代码示例（代码块）**必须使用 Java 语言**，包括算法、数据结构、系统设计等所有技术主题的示例代码。

正确：

````markdown
```java
public int[] twoSum(int[] nums, int target) {
    Map<Integer, Integer> map = new HashMap<>();
    for (int i = 0; i < nums.length; i++) {
        int complement = target - nums[i];
        if (map.containsKey(complement)) {
            return new int[]{map.get(complement), i};
        }
        map.put(nums[i], i);
    }
    return new int[]{};
}
```
````

错误（禁止出现 Python、C++ 等非 Java 代码）：

````markdown
```python
def two_sum(nums, target):
    ...
```
````

## 十一、代码注释规范

代码示例中，**关键逻辑处必须添加注释**，说明「为什么这样做」而非「做了什么」。注释应简洁明了，每段关键逻辑一处即可，不追求逐行注释。

正确示例：

```java
public int findPeakElement(int[] nums) {
    int left = 0, right = nums.length - 1;
    while (left < right) {
        int mid = left + ((right - left) >>> 1);
        // 如果中间值大于右侧，说明峰值在左边（含中间）
        if (nums[mid] > nums[mid + 1]) {
            right = mid;
        } else {
            // 否则峰值在右边
            left = mid + 1;
        }
    }
    return left;
}
```

错误示例（无注释或逐行废话注释）：

```java
public int findPeakElement(int[] nums) {
    int left = 0, right = nums.length - 1;  // 初始化左右指针
    while (left < right) {                   // 循环条件
        int mid = left + ((right - left) >>> 1);  // 计算中点
        // ...
    }
    return left;  // 返回结果
}
```

> 核心原则：注释要解释**意图和 Trade-off**，不要解释代码本身在做什么。代码已经能说明「做什么」，注释要说「为什么这样做」和「注意什么坑」。

> 特别说明：若文档主题涉及特定语言的对比（如「Go vs Java」），可在对比部分出现其他语言代码，其余正文必须以 Java 为主。

### 标题衔接

VitePress 默认 h2 标题自带顶部实线分隔（`border-top`），无需额外写 `---`。如需在标题之间使用视觉分隔，在内容段落之后手动写 `---` 即可。

**正确**：标题之间有内容段落即可，分隔线由 CSS 自动提供。

**错误**：

```markdown
# 一级标题

---

## 二级标题
```

**可接受**：两个章节之间有过渡文字，再加分隔线。

---

## 常见问题

**Q: 追求趣味性会不会影响技术准确性？**

A: 不会。趣味性是表达方式，技术准确性是内容底线。两件事不冲突——一个好的厨师，既能做出美味的菜，又能保证食品安全。

**Q: 不同模块可以用不同风格吗？**

A: 当然可以。「Java 并发」可以用专家型的严谨，「Redis 实战」可以用战友型的直接，「系统设计」可以用智者型的宏观。风格服务于内容，不是限制内容。

**Q: 如何避免「为了有趣而有趣」？**

A: 问自己一个问题：这段「有趣」的内容，有没有让读者更好地理解技术？如果没有，那就是废话，该删就删。
