---
description: VitePress 知识库文档编写规范 - 强调风格差异化、故事感与趣味表达
globs: docs/**/*.md
alwaysApply: true
---

# VitePress 知识库文档编写规范

> 核心目标：打破模板化写作，每篇文档都有独特的「面孔」和「气质」

---

## 风格定位：找到文档的「人设」

写作前先想清楚：这份文档想扮演什么角色？

### 五种风格画像

| 风格 | 适合内容 | 语言特点 | 典型语气 |
|------|----------|----------|----------|
| **教练型** | 入门教程、技能学习 | 鼓励性、耐心、循序渐进 | 「让我们一起...」「你也可以做到」 |
| **专家型** | 原理剖析、源码解读 | 严谨、深入、术语准确 | 「这是因为...」「从底层看...」 |
| **战友型** | 问题排查、实战经验 | 共情、务实、直接 | 「踩过这个坑」「血的教训」 |
| **向导型** | 配置指南、操作步骤 | 清晰、高效、零废话 | 「只需三步」「直接执行」 |
| **智者型** | 设计思想、架构哲学 | 思辨、宏观、启发性 | 「本质是...」「换个角度看...」 |

### 风格选择原则

```
入门教程 → 教练型
原理讲解 → 专家型 / 智者型
问题解决 → 战友型
配置操作 → 向导型
架构设计 → 智者型
```

> **不要**：整站文档都是同一种语气，同一套句式

---

## 开头设计：抓住读者的「前 3 秒」

开头决定了读者是否继续阅读。根据内容特点选择合适的开场方式。

### 开头方式库

#### 1. 场景切入型
```markdown
凌晨 2 点，你正准备上线新功能，发现数据库连接池耗尽...

# 或者
办公室里，同事问你：「这条 SQL 为什么要加索引？不加不行吗？」
```

#### 2. 痛点共鸣型
```markdown
你一定遇到过：配置文件改了无数遍，程序就是不起作用。
这通常不是代码的问题，而是你忽略了某个启动参数。
```

#### 3. 核心追问型
```markdown
# 一条简单的 SELECT 语句，MySQL 是怎么找到你的数据的？

大多数人不关心这个问题，直到需要排查慢查询...
```

#### 4. 对比反差型
```markdown
有些人写 SQL 十年，还是只会 `SELECT *`。
有些人工作三年，已经能清晰说出这条查询的执行计划。
区别不在于工作时间，在于是否理解 MySQL 的索引机制。
```

#### 5. 数字锚定型
```markdown
90% 的慢查询问题来自这 3 种写法。
掌握它们，能让你的查询快 100 倍。
```

#### 6. 历史/故事型
```markdown
2014 年，James Gosling 在一次访谈中回忆：
Java 最初的名字是「Green」，目标是解决消费电子设备的编程问题。
谁能想到，这个为微波炉设计的语言，后来统治了企业级开发？
```

#### 7. 直观演示型
```markdown
# 先看效果

这是没有索引的查询结果：

```
查询耗时：15.2 秒，扫描了 100 万行
```

这是加了索引后的结果：

```
查询耗时：12 毫秒，使用了主键索引
```

怎么做到的？
```

#### 8. 直接定义型（慎用）
```markdown
> 如果你赶时间，直接看第三部分「快速解决」

InnoDB 是 MySQL 默认的存储引擎，支持事务、行级锁和外键约束。
```

### 开头避坑

- ❌ 「随着互联网的发展...」「在当今社会...」—— 大空话
- ❌ 「本文将介绍...」「本节讲解...」—— 无效信息
- ❌ 长篇铺垫后才进入正题 —— 消耗读者耐心
- ❌ 开头贴「元信息标签」：如 `> JDK 17+ | 新手友好 | 边学边练`
  - 一堆标签像免责声明，容易劝退读者
  - 版本要求可以融入「快速验证」或「版本选择」模块
  - 如果要用引用块开头，用**简短的一句话背景描述**即可，如：
    - `> 适合零基础学习者`（简单说明受众）
    - `> 这是一个常见的面试问题`（说明重要性）
    - `> 凌晨 2 点，服务器又 OOM 了...`（用场景切入）
  - 核心原则：**让读者想继续看，而不是先告知一堆门槛**

---

## 结构设计：让信息「流动」起来

好的结构不是固定的模板，而是让信息自然呈现的路径。

### 五种结构模型

#### A. 漏斗型（推荐：概念类）
```
大领域 → 核心问题 → 关键概念 → 细节展开 → 实践应用
例：并发编程 → 线程安全 → synchronized → 8种用法 → 性能对比
```

#### B. 阶梯型（推荐：教程类）
```
第一步（能跑通）→ 第二步（功能完整）→ 第三步（优化完善）→ 第四步（生产级）
特点：每步都有可运行的成果
```

#### C. 矩阵型（推荐：对比/选型类）
```
        方案A    方案B    方案C
维度1   ✅       ⚠️       ❌
维度2   ⚠️       ✅       ✅
维度3   ✅       ✅       ⚠️
结论 → 根据场景选择
```

#### D. 网格型（推荐：工具/框架类）
```
核心功能 → 场景1用法 → 场景2用法 → 场景3用法
                    ↕
              常见问题
```

#### E. 循环型（推荐：原理讲解）
```
问题提出 → 初步尝试 → 发现局限 → 解决方案 → 新问题 → 再次优化...
特点：还原思考过程，适合讲解「为什么会这样设计」
```

### 结构选择建议

| 文档类型 | 推荐结构 | 原因 |
|----------|----------|------|
| 入门教程 | 阶梯型 | 循序渐进，每步有成果 |
| 概念原理 | 漏斗型 | 从宏观到微观 |
| 配置操作 | 网格型 | 按场景查找 |
| 问题排查 | 漏斗型（逆向） | 从现象到原因 |
| 技术选型 | 矩阵型 | 对比清晰 |
| 源码解析 | 循环型 | 还原设计思路 |

---

## 节奏控制：给文档「呼吸感」

### 快节奏段落
适用于：背景介绍、配置步骤、重复性内容

```markdown
### 安装 MySQL 步骤

1. 下载 MySQL 8.0 安装包
2. 运行安装程序，配置 root 密码
3. 验证安装：`mysql --version`
```

### 慢节奏段落
适用于：核心概念、重要原理、需要消化的地方

```markdown
### B+ 树索引的本质是什么？

不是「加速查询」这么简单。

当你创建一个索引，MySQL 会在磁盘上构建一棵 B+ 树。
这棵树的特点是：所有数据都存在叶子节点，非叶子节点只存索引值。

但问题来了：如果数据量很大，这棵树会变得非常深吗？

选项 A：树会变得非常深，查询变慢
选项 B：树通过扇出控制深度，查询稳定
选项 C：数据太多，索引会失效

MySQL 通过高扇出和压缩层数来保持稳定，但这个选择背后有很多权衡...
```

### 节奏设计原则

```
核心章节 → 慢节奏 + 详细解释 + 代码演示
辅助章节 → 快节奏 + 简洁说明 + 表格总结
过渡章节 → 极快 + 引导语 + 链接
```

---

## 表达技巧：让文字「活起来」

### 善用对比

```markdown
# 反面示例 vs 正面示例

-- ❌ 不好：SELECT *
SELECT * FROM users WHERE id = 1;

-- ✅ 好：只查需要的字段
SELECT id, name, email FROM users WHERE id = 1;
```

### 善用类比

```markdown
B+ 树的插入操作，就像在一本有序字典中插入新词条：
当某一页写满了，就拆成两页。
但不是随机拆分，而是保持字母顺序。

小字典还好，大字典就需要经常翻页（磁盘 I/O）。
这也是为什么索引不是越多越好——每个索引都是一棵独立的 B+ 树。
```

### 善用数字

```markdown
# 不要
"索引能提高查询效率"

# 要
"索引可以将查询时间从 O(n) 降低到 O(log n)，100 万条数据只需约 20 次比较"
```

### 善用引语

```markdown
> "我蔑视那些不写注释的程序员，也蔑视那些解释为什么要喝水的鱼。" —— 某人
>
> 但说实话，Java 源码里有些注释是真的该看...
```

### 善用转折

```markdown
大多数教程会告诉你怎么创建索引。
但我更想告诉你：**什么时候不要创建索引**。

因为过多的索引会拖慢写入速度，占用额外磁盘空间。
```

### 善用故事感

技术文档不是说明书，它可以是会讲故事的。

#### 1. 给概念一个「身世」

每个技术点都有它的来龙去脉，讲清楚「它从哪里来」「为什么会诞生」「解决了什么问题」，比直接列 API 更有意思。

```markdown
# HashMap 为什么选择「拉链法」而不是「开放地址法」？

这个问题要从 HashMap 的创造者 Doug Lea 说起...

实际上，选择拉链法背后有三个现实考量：
1. 真实数据往往有热点分布...
2. 开放地址法在删除操作上很棘手...
3. 链表可以优雅地处理哈希冲突...

但你知道吗？JDK 8 之后，拉链法升级了，变成了「红黑树 + 链表」的双保险...
```

#### 2. 给问题一个「受害者」

读者需要代入感。没有人愿意听「慢查询会影响性能」，但每个人都愿意听「凌晨 3 点，你的用户无法下单，只因为一条没优化的 SQL」。

```markdown
# 那个价值 100 万的慢查询

2019 年，某电商平台在双十一零点促销时，订单接口突然全面超时。

排查了 2 小时，最后发现是一条简单的 SELECT * 查询，
在订单表超过 1000 万数据后，全表扫描导致响应时间超过 30 秒。

没有 LIMIT，没有索引，没有分页，
只有一个安静的 `Using filesort`。

那晚的损失是：每分钟 100 万的交易额。
```

#### 3. 给解决方案一个「对比」

通过前后对比，让读者感受到改变的价值。

```markdown
# 慢查询优化：从 15 秒到 12 毫秒

优化前：用户查询订单列表，页面转圈 15 秒

```sql
SELECT * FROM orders WHERE user_id = 123;
```

优化后：同样的查询，12 毫秒返回

```sql
-- 方案一：添加索引
ALTER TABLE orders ADD INDEX idx_user_id (user_id);

-- 方案二：优化 SQL
SELECT order_id, order_time, total_amount
FROM orders
WHERE user_id = 123
ORDER BY order_time DESC
LIMIT 20;

-- 方案三：分页查询
SELECT * FROM orders
WHERE user_id = 123 AND order_id < #{lastOrderId}
LIMIT 20;
```

三选一？还是组合使用？往下看。
```

#### 4. 给代码一个「人设」

让 SQL「说话」，让它有自己的性格和态度。

```markdown
# 这条 SQL，不该出现在生产代码里

```sql
SELECT * FROM orders WHERE status = 'pending';
```

这不是查询，这是一个全表扫描的邀请函。
```

---

## 代码呈现：不仅仅是「贴代码」

### 渐进式代码展示

```markdown
### 从最简单的开始

```sql
-- 起点：这样写有什么问题？
SELECT * FROM users;
```

### 加上条件过滤

```sql
-- 优化：只查需要的字段和条件
SELECT id, name, email FROM users WHERE status = 'active';
```

### 最终版本

```sql
-- 完整查询：字段过滤 + 条件 + 排序 + 分页
SELECT id, name, email, created_at
FROM users
WHERE status = 'active' AND created_at > '2024-01-01'
ORDER BY created_at DESC
LIMIT 100;
```

### 代码中的「对话」

```sql
-- 不要：直接给代码
SELECT * FROM users WHERE id = 1;

-- 要：代码 + 注释 + 说明
SELECT id, name, email  -- 只查需要的字段，减少网络传输
FROM users             -- 从用户表
WHERE id = 1;          -- 主键查询，利用索引
```

---

## 结尾设计：让读者「带走」什么

### 结尾类型

#### 1. 总结收束型
```markdown
## 要点回顾

今天我们学到了：
1. 索引的底层是**B+ 树**
2. 索引可以加快查询，但会占用额外空间
3. 适合**等值查询**，不适合**全表扫描**

记住这个选择口诀：想快就加索引，想省空间就优化查询。
```

#### 2. 行动指引型
```markdown
## 下一步

- [ ] 在测试环境执行今天的 SQL
- [ ] 用 EXPLAIN 分析一条慢查询
- [ ] 想想它和 JOIN 的区别
```

#### 3. 留白思考型
```markdown
## 留给你的问题

B+ 树的深度通常是 3-4 层，可以存储上亿条数据。

但为什么数据量大了之后，查询还是会变慢？

这个问题，留给你思考。
```

#### 4. 承上启下型
```markdown
现在你理解了索引的基本原理。

但面试官不会只问你「索引怎么创建」。
他们更关心：**索引为什么会失效？**

下一节，我们聊聊索引失效的 11 种情况。
```

#### 5. 金句升华型
```markdown
最后送你一句话：

> "好的 API 设计，是让使用者不需要看文档。"
>
> 好的代码注释，是让未来的自己感谢现在的自己。

共勉。
```

---

## 差异化检查清单

写完文档后，检查是否有「模板感」：

- [ ] **开头是否独特？** 能否去掉后直接放到另一篇文章开头？
- [ ] **节奏是否变化？** 全文是否都是同一个语速？
- [ ] **结构是否合理？** 是否只是「背景-原理-用法-总结」？
- [ ] **是否有个人印记？** 能否看出是谁写的？
- [ ] **是否有记忆点？** 读者离开后能记住什么？

---

## 内容质量底线

无论风格如何变化，以下标准不变：

| 标准 | 要求 |
|------|------|
| **准确性** | 技术细节必须正确，不确定的内容标注适用范围 |
| **完整性** | 核心知识点必须有「是什么-为什么-怎么用」 |
| **可运行** | 代码示例必须能直接运行 |
| **术语解释** | 首次出现的术语必须附带解释 |
| **避免模糊** | 不使用「可能」「大概」「也许」 |
| **标题衔接** | 相邻标题之间必须有内容，避免出现连续横线 |

---

## 排版细节规范

### 标题衔接规则

VitePress 会将 `---` 渲染为横线。如果两个标题之间没有内容直接用 `---` 分隔，页面会显示两条难看的横线。

**正确做法：** 标题之间必须有内容段落，或者去掉分隔线。

```markdown
# 一级标题

正文内容，解释什么是什么。

## 二级标题

正文内容，说明如何使用。
```

**错误做法：** 两级标题之间直接加 `---`。

```markdown
# 一级标题

---

## 二级标题
```

**可接受的做法：** 如果两个章节确实需要视觉分隔，在 `---` 前留一段过渡文字。

```markdown
## 章节 A

内容...

掌握了这些，我们来看下一部分。

---

## 章节 B

继续内容...
```

**原则：一级和二级标题之间要么有内容，要么没有分隔线。**

---

## 常见问题

**Q: 差异化是否意味着可以随意写？**

A: 不是。差异化是表达方式的多样化，不是内容的随意化。核心原则（准确性、完整性）必须遵守。

**Q: 如何找到自己的风格？**

A: 多读优秀技术博客，有意模仿，然后慢慢融入自己的特点。DBInsight 不要求统一风格，但要求每篇都有用心打磨的痕迹。

**Q: 风格太「个性」会不会影响阅读？**

A: 有可能。所以要在「个性」和「清晰」之间找平衡。原则是：**读者能轻松理解，技术内容无误，表达方式有特色。**
