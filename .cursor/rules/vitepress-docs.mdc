---
description: VitePress 文档技术规范 — 链接规则、文件结构、代码规范
globs: docs/**/*.md
alwaysApply: true
---

# VitePress 文档规范

## 链接规则

### 绝对路径 vs 相对路径

- 使用 `/module/xxx` 格式的绝对路径，而非相对路径
- 目录跳转使用 `/directory/index` 而非 `/directory`

### 跨模块链接格式

```markdown
# 正确
[Java 基础](/java/index)
[MySQL 索引](/database/mysql/index)
[Spring IoC](/framework/spring/index)

# 错误
[Java 基础](../java/index)
[Java 基础](./java/index)
[/java/index]
```

### 导航栏对应模块链接

| 导航项 | 链接格式 |
|-----|-----|
| 面试准备 | `/interview/xxx` |
| Java | `/java/xxx` |
| 计算机基础 | `/cs/xxx` |
| 数据库 | `/database/xxx` |
| 常用框架 | `/framework/xxx` |
| 分布式架构 | `/distributed/xxx` |
| 高性能与高可用 | `/high-performance/xxx` |
| 中间件 | `/middleware/xxx` |
| 工程化与运维 | `/devops/xxx` |

## 代码规范

### 泛型转义

代码中的泛型类型需要使用 HTML 转义符，避免被解析为 HTML 标签：

```markdown
# 正确
`List&lt;String&gt;`
`Map&lt;String, Integer&gt;`
`&lt;T&gt;`

# 错误
`List<String>`
`Map<String, Integer>`
`<T>`
```

### 反引号包裹

- 单行代码片段使用反引号 \`
- 多行代码使用三反引号 \`\`\`

## 文件结构

### 必需文件

- 每个模块目录必须有 `index.md`
- 目录名使用 kebab-case（小写加连字符）

### Sidebar 同步

- 添加新模块到导航栏时，同时更新 `docs/.vitepress/nav.js`
- 添加新 sidebar 时，同步更新 `docs/.vitepress/sidebar.js`
- 创建对应的 sidebar 文件 `docs/.vitepress/sidebar/[module].js`

## 新增文档检查清单

- [ ] 文件放在正确的模块目录下
- [ ] 链接使用绝对路径 `/module/xxx`
- [ ] 导航栏已添加对应入口（如需要）
- [ ] Sidebar 已更新（如需要）
- [ ] 代码中的泛型使用转义符
- [ ] 运行 `npm run build` 验证无死链接
