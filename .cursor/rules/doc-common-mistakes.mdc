---
description: Java 面试文档常见错误与避坑指南
globs: docs/**/*.md
alwaysApply: true
---

# 文档常见错误避坑指南

> 核心目标：避免技术文档中的常见错误，确保知识准确性和可读性

---

## 一、技术知识错误

### 1.1 Java 基础常见错误

| 错误说法 | 正确说法 | 说明 |
|---------|---------|------|
| String 是基本数据类型 | String 是引用类型 | String 在 Java 中是类，不是基本类型 |
| new String("abc") 创建了两个对象 | 不一定 | 如果字符串常量池中已有 "abc"，只创建 1 个对象；否则创建 2 个 |
| HashMap 是线程安全的 | HashMap 线程不安全 | JDK 7 及之前存在死循环问题，JDK 8 已修复但仍非线程安全 |
| HashMap 初始容量是 16 | 初始容量是 16，负载因子是 0.75 | 默认容量为 16，但构造函数可指定初始容量 |
| ArrayList 线程安全 | ArrayList 线程不安全 | Vector 线程安全，但性能较差；推荐 CopyOnWriteArrayList |
| 尽量使用接口引用具体实现 | 大部分情况下是对的 | 涉及类型特定行为时，直接使用实现类更合适 |

### 1.2 JVM 常见错误

| 错误说法 | 正确说法 | 说明 |
|---------|---------|------|
| Java 8 之后没有永久代 | JDK 8 用元空间替代了永久代 | 元空间使用本地内存，不再受 JVM 堆大小限制 |
| 死锁就是线程阻塞 | 死锁是线程间相互等待对方持有的锁 | 线程可以处于 BLOCKED 状态但不是死锁 |
| Minor GC 和 Full GC 指新生代和老年代 | 这个说法不准确 | Minor GC 清理年轻代，但 Full GC 清理整个堆，包括年轻代、老年代和元数据区域 |
| 对象一定在堆上分配 | 不一定 | 逃逸分析优化后，标量替换可能在栈上分配 |
| 偏向锁在 JDK 15 后默认关闭 | 偏向锁在 JDK 15 后被废弃 | JDK 15 开始默认禁用偏向锁，JDK 18 完全移除了偏向锁机制 |

### 1.3 并发编程常见错误

| 错误说法 | 正确说法 | 说明 |
|---------|---------|------|
| synchronized 性能很差 | 现代 JVM 中 synchronized 优化良好 | 锁升级机制、无锁优化等使 synchronized 性能大幅提升 |
| volatile 可以替代锁 | volatile 不能替代锁 | volatile 只保证可见性和有序性，不能保证原子性 |
| ThreadLocal 一定内存不泄露 | ThreadLocal 可能引发内存泄漏 | 弱引用保护 + 线程池场景下仍需主动 remove |
| 线程池的核心线程不会被回收 | 当 allowCoreThreadTimeOut 为 true 时核心线程也会回收 | 核心线程默认不会被回收，除非设置允许超时 |

### 1.4 MySQL 常见错误

| 错误说法 | 正确说法 | 说明 |
|---------|---------|------|
| 索引越多越好 | 索引不是越多越好 | 每次写入都要维护索引，增加写入开销，占用磁盘空间 |
| 联合索引可以从中间开始用 | 必须遵循最左前缀原则 | 从最左边的列开始使用，否则索引无法生效 |
| SELECT * 永远不用 | 在需要所有字段时 SELECT * 是最优的 | 如果只需要部分字段，明确列出更优（减少网络传输、避免回表） |
| count(*) 和 count(1) 完全一样 | count(1) 略快 | 在 InnoDB 中，count(*) 经过优化，count(1) 需要判断表达式 |
| 加了索引查询就一定快 | 不一定 | 查询条件不匹配索引、数据量太小、统计信息不准确等情况会导致全表扫描 |

### 1.5 Redis 常见错误

| 错误说法 | 正确说法 | 说明 |
|---------|---------|------|
| Redis 是纯内存数据库 | Redis 默认是内存数据库，支持持久化 | 可配置 RDB、AOF 或混合持久化 |
| Redis 是单线程所以很慢 | 单线程指的是网络 I/O 和命令执行 | CPU 不是瓶颈，瓶颈在网络；多线程用于后台操作（如持久化、懒删除） |
| Redis 事务可以回滚 | Redis 事务不支持回滚 | Redis 事务不满足 ACID，只保证批量执行， WATCH 是乐观锁机制 |
| Redis Cluster 比哨兵好 | 看场景 | 小规模主从选哨兵，大规模分片选集群 |
| Redis 适合存储所有类型数据 | 不是 | 适合简单 K-V、计数器、缓存等；复杂查询场景用传统数据库 |

---

## 二、表述问题

### 2.1 容易产生歧义的地方

1. **时间复杂度表述**
   - 错误：HashMap 查找是 O(1)
   - 正确：HashMap 在理想情况下查找是 O(1)，最坏情况下（哈希冲突严重）是 O(n)

2. **空间复杂度表述**
   - 错误：快速排序空间复杂度是 O(n log n)
   - 正确：快速排序空间复杂度是 O(log n)（递归栈），最坏情况是 O(n)

3. **版本差异**
   - 错误：HashMap 在 JDK 8 之后使用红黑树
   - 正确：HashMap 在 JDK 8 及之后，链表长度超过 8 且数组长度大于等于 64 时转为红黑树

### 2.2 夸大其词的问题

1. **性能断言**
   - 错误：「使用索引后查询快 100 倍」
   - 正确：「使用索引后查询从 O(n) 降为 O(log n)，在 100 万数据量下约需 20 次比较」

2. **可靠性保证**
   - 错误：「消息队列保证消息不丢失」
   - 正确：「配置适当的持久化和确认机制，可以将消息丢失概率降到极低，但无法完全消除」

---

## 三、排版与格式问题

### 3.1 代码规范

1. **泛型未转义**
   - 错误：`List<String>`
   - 正确：`List&lt;String&gt;`

2. **代码块缺少语言标记**
   - 错误：\`\`\` 后面不加语言名
   - 正确：\`\`\`java

3. **行内代码与普通文字间距**
   - 错误：`这是List&lt;String&gt;类型`
   - 正确：`这是 \`List&lt;String&gt;\` 类型`

### 3.2 链接规范

1. **死链接**
   - 确保链接目标文件存在
   - 使用绝对路径：`/java/index` 而不是 `../java/index`

2. **链接文本不明确**
   - 错误：`点击[这里](/java/index)查看`
   - 正确：`点击[Java 面试知识体系](/java/index)查看`

### 3.3 标题层级

1. **连续跳级**
   - 错误：`# 一级 → ### 三级`
   - 正确：`# 一级 → ## 二级 → ### 三级`

2. **标题间缺少内容**
   - 错误：`## 标题1` 直接接 `## 标题2`
   - 正确：两个标题之间至少有一行正文或分隔线

---

## 四、内容质量问题

### 4.1 常见逻辑错误

1. **因果关系混乱**
   - 错误：`因为 HashMap 是线程不安全的，所以不能用于高并发场景`
   - 正确：`HashMap 线程不安全，高并发场景建议使用 ConcurrentHashMap`

2. **条件判断不完整**
   - 错误：`数据库使用索引后性能一定提升`
   - 正确：`在数据量较大、查询条件明确时，使用索引可以提升性能；但对于小表或全表扫描更优的场景，索引可能适得其反`

### 4.2 缺少适用场景说明

技术方案都有其适用范围，需要说明：

1. **什么时候用？**
2. **什么时候不用？**
3. **有什么替代方案？**

---

## 五、面试相关错误

### 5.1 面试问题答案错误

1. **synchronized 锁的是什么？**
   - 错误：`synchronized 锁的是代码块`
   - 正确：`synchronized 可以修饰代码块（锁对象）或方法（锁 this/类对象），锁的是对象`

2. **volatile 是怎么保证可见性的？**
   - 错误：`volatile 通过总线嗅探技术保证可见性`
   - 正确：`volatile 通过内存屏障实现可见性，总线嗅探是实现的一种机制`

3. **MySQL 事务隔离级别默认是什么？**
   - 错误：`MySQL 默认是可重复读（RR）`
   - 正确：`MySQL InnoDB 默认隔离级别是可重复读（RR），Oracle 默认是读已提交（RC）`

### 5.2 面试追问方向遗漏

面试中会深入考察以下方面：

1. **HashMap**
   - JDK 7 扩容时为什么会导致死循环？（头插法导致链表倒置）
   - 为什么负载因子是 0.75？（空间和时间权衡）
   - 扰动函数的设计意图？（减少哈希碰撞）

2. **synchronized**
   - 锁升级的过程？（偏向锁 → 轻量级锁 → 重量级锁）
   - 锁升级的原理？（偏向锁：CAS + Mark Word，轻量级锁：CAS + 自旋，重量级锁：系统互斥量）
   - wait/notify 的原理？（等待队列与同步队列）

3. **MySQL**
   - MVCC 在 RR 和 RC 下的区别？（RR 下 ReadView 在事务开始时创建，RC 下每次读取都创建）
   - 间隙锁锁的是什么？（索引记录之间的间隙）

---

## 六、检查清单

写完文档后，用这份清单核对：

### 知识准确性
- [ ] 技术定义是否准确？
- [ ] 边界条件是否考虑？
- [ ] 版本差异是否说明？
- [ ] 性能断言是否有数据支撑？

### 表述清晰性
- [ ] 是否避免歧义？
- [ ] 代码是否规范？
- [ ] 链接是否正确？

### 面试实用性
- [ ] 面试常考点是否覆盖？
- [ ] 可能的追问方向是否预判？
- [ ] 是否有实用的对比表格？
